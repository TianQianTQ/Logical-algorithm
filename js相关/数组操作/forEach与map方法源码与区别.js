/** * forEach */var arr = [1,2,3,4,5];Array.prototype.myForEach = function(fn){    var len = this.length;    for(var i = 0;i<len;i++){        fn(this[i],i);    }}arr.myForEach(function(ele,index){    console.log(ele,index);})/** * map */Array.prototype.myMap = function(fn){    var len = this.length;    var arr = [];    for(var i = 0; i<len ;i++){        arr.push(fn(this[i],i))    }    return arr;}var arr2 = [1,2,3,4,5,6];var res = arr2.myMap(function(value,index){    return value * 2;})console.log(res)/** * 相同点：循环遍历数组中的每一项，匿名函数参数相同，this指向调用方法的数组，只能遍历数组 * 不同点: map创建了新数组，不改变原数组，forEach可以改变原数组 *          map按照元素顺序依次处理，forEach遍历时跳过空缺 */