/** * 格式化当前时间 如 2016-12-21 10:00:00 * @param {String} time Date * @return {String} str */function formatDate(time) {    let d;    let h;    let m;    let mm;    let _date;    let s;    let y;    _date = time ? new Date(time) : new Date();    y = _date.getFullYear().toString();    mm = twodigit(_date.getMonth() + 1);    d = twodigit(_date.getDate());    h = twodigit(_date.getHours());    m = twodigit(_date.getMinutes());    s = twodigit(_date.getSeconds());    return `${y}-${mm}-${d} ${h}:${m}:${s}`;}/** * 双位数字 小于10补0 * @param {Number} val val * @return {String} str */function twodigit(val) {    if (val < 10) {        return `${'0' + ''}${val}`;    }    return val;}